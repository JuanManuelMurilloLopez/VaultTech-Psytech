<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Respuestas de formato de entrevista</title>
    <!-- Bootstrap CSS -->
    <link href=
    "https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet">
    <!-- Link css -->
    <link rel="stylesheet" href="/css/psicologos/respuestasFormatoEntrevista.css">
    <!-- CSS navside -->
    <link rel="stylesheet" href="/css/includeCss/_navsidePsicologo.css">
    <!-- CSS cerrar sesión -->
    <link rel="stylesheet" href="/css/includeCss/_buttonCerrarSesion.css">
</head>
<body>

    <div class="main">

        <!-- Navside -->
        <%- include('../includes/_navsidePsicologo.ejs') %>

        <div class="container">

            <!-- Boton de cerrar sesión -->
            <%- include('../includes/_buttonCerrarSesion.ejs') %>

            <h1 class="text-center"> Respuestas Formato de Entrevista </h1>

            <% let seccionActual = null; %>
            <% for (respuesta of respuestasAspirante) { %> 
                <% if (respuesta.seccion !== seccionActual) { %>
                    <% if (seccionActual !== null) { %>
                        </div>
                    <% } %>
                    <h5 class="titulo-seccion"><%= respuesta.seccion %></h5>
                    <hr><br>
                    <div class="mb-3">
                <% } %>
                <div class="mb-3 ">
                    <h5 class="pregunta-titulo"><%= respuesta.nombrePreguntaFormatoEntrevista %></h5>
                    <p class="pregunta-respuesta"><%= respuesta.respuestaAspirante %></p>
                </div><br>
                <% seccionActual = respuesta.seccion; %>
            <% } %>
            <% if (seccionActual !== null) { %>
                </div>
            <% } %><br><br><br>


            <div class="fixed-bottom-buttons">
                <div class="container">
                    <div class="row gx-3 mt-4 mb-3">
                        <div class="col-md-6">
                            <a href="/psicologo/aspirantes/<%= aspirante %>/<%= grupo %>/<%= idInstitucion %>" 
                            class="btn btn-color--third">Regresar</a>
                        </div>
    
                        <div class="col-md-6 text-end">
                            <a href="/psicologo/informacion-familiares/<%= grupo %>/<%= aspirante %>/<%= idInstitucion %>" 
                            class="btn btn-color--secondary">Informacion Familiar</a>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<!-- Incluir el script del navside -->
<script src="/js/_navside.js"></script>
    
</body>
</html>